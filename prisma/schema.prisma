    id          String   @id @default(cuid())
    title       String
    description  String?
    thumbnail   String?
    categoryId   String?
    published   Boolean   @default(false)
    authorId    String
    author      User      @relation(fields: [authorId], references: [id], onDelete: Cascade)
    servers     Server[]
    playlistUrl String? // Optional M3U playlist URL
    playlists   Playlist[] // Reference to playlists created from this stream
    ads         Ad[]
    status       String   @default("unknown") // online, offline, unknown
    statusCheckedAt DateTime? // Last time status was checked
    quality      String? // hd, fhd, sd, 4k
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt